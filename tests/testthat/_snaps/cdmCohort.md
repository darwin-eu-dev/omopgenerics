# test create omop_cohort

    class of cohort objects must be the same:
    ** class(cohort) = tbl_sql, tbl_df, tbl, data.frame
    ** class(cohort_set) = tbl_df, tbl, data.frame
    ** class(cohort_attrition) = tbl_df, tbl, data.frame

