test_that("test cdm_reference", {
  # expect_no_error(cdm <- mockCdm())
  # expect_true(class(cdm) == "cdm_reference")
  # expect_no_error(cdmName(cdm))
  # expect_no_error(print(cdm))
  # expect_no_error(cdm[["random"]])
  # expect_no_error(cdm$random)
})
